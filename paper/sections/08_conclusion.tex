\section{Conclusion}
\label{sec:conclusion}

% Target: 0.5 page (~400 words). Synthesize. Write last.

\todo{Three short paragraphs:
\begin{enumerate}
  \item Restate the two findings — reproducibility failure + metric blindness
    — in one sentence each.
  \item Position the methodological contribution: \S6 stress tests (if kept)
    or, if §6 was cut, a forward-looking call to develop pattern-structure
    metrics for time-series generators.
  \item Implications: financial time-series generation is a rare domain where
    the underlying data is itself near-iid (returns are nearly white noise),
    so generators can pass marginal tests with very little signal. Evaluating
    them requires testing the temporal/pattern structure they're supposed to
    add, not just the marginal they share with the real data anyway.
\end{enumerate}}
